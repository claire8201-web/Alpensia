# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['Alpensia_V4.1.1.py'],
    pathex=[],
    binaries=[],
    datas=[('alpensia_logo.png', '.'), ('alpensia_logo.ico', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Alpensia',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['alpensia_logo.ico'],
)
